import { redirect, NOT_FOUND } from 'redux-first-router';
import Feed from './components/Feed/feed.jsx';

export default {
  HOME: '/',
  PROFILE: '/profile',
  FEED: '/feed',
  FOLLOW: '/follow',
  LOGIN: '/login',
  SIGNUP: '/signup',
  LOGOUT: '/logout',
};
